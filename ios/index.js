import { NativeModules } from "react-native";

const { RNInnerFence } = NativeModules;

export default RNInnerFence;

import { NativeModules } from "react-native";

const { RNInnerFence } = NativeModules;

export default RNInnerFence;
